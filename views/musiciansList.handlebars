
  <div class="container p-1">
    <div class=" my-2 p-2 border-top border-bottom page-switcher">
      <a href="/members/list/bands" class="btn btn-dark mx-5 ">Bands Near You</a>
    </div>
    <div class="row d-flex flex-row justify-content-between">
      
      <div class="col-md-7 my-5 p-0 border-right border-left text-center" id="left">

   {{!-- NAZ: This is where all of the musicians will be desplayed. Changed the hard coding into dynamic    --}}
     {{!-- ************************************************************************************************************* --}}


      <h2 class="mb-5">Musicians In Your Area</h2>
         {{#each musicians}} 
        <div class="row my-3 p-3 border-top d-flex flex-row align-items-center">
            <div class="col-md-2">
            <img src="{{avatar}}" alt="user avatar" width ="100%">
            </div>
            <div class="col-md-8">
              <h5>{{nickname}}</h5>
              <p>{{description}}</p>
            </div>
            <div class="col-md-2">
              <a href="/members/explore/{{id}}" class="btn btn-secondary">Explore</a>
            </div> 
        </div>
         {{/each}}
      </div>
    {{!-- **************************************************************************************************************** --}}
      <div class="col-md-4 my-2 p-3 border">
         <h3>:) Welcome, </h3><h4> {{currentUser.email}}</h4>
                 {{!-- Naz: This route will allow user to view his/her messages
                  ************************************************************* --}}
                  <a href="/members/messages/{{currentUser.id}}">Messages</a>
                  <hr>
                  {{!-- *************************************************************** --}}
           <a class="btn btn-outline-danger my-5" href="/logout">
              Logout
           <a>
        <h4>Make yourself known with a detailed account!</h4>
        <hr>
        {{#if needAcc}}
           <a class="btn btn-outline-warning my-2" href="/members/self/{{currentUser.id}}">
              View Account Details
           <a>
        {{else}}
           <a class="btn btn-outline-success my-2" href="/members/{{currentUser.id}}">
              Create Personal Account
           <a>
          {{/if}}

      </div>
    </div>
  </div>
