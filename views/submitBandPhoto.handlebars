<div class="container-fluid">
    <div class="row d-flex flex-row justify-content-between">
        
        <div class="col-md-10 p-5">
            <h1 class="mb-5">Submit a professional photo of your band</h1>
            </form >
            <form action="/upload/band/{{currentUser.id}}" method="POST" enctype="multipart/form-data" >
                <div class="form-group">
                  <label >Upload here</label>
                  <input name="myImage" type="file" class="form-control-file" id="upload-image">
                </div>
                <button type="submit" class="btn btn-primary my-2">Upload</button>
            </form>
        </div>
        <div class="col-md-2">
            <a class="btn btn-outline-danger my-5" href="/logout">
                Logout
            <a>
            <a class="btn btn-outline-success my-5" href="/members">
                Back
            <a>
        </div>
    </div>
</div>
